#!/usr/bin/perl
%hash = ();
while(<>) { # 对于输入文件的每一行
    chomp(); # 去除行尾换行符
    $_ =~ s/\\/\//g; # 把\替换成/，如果想区分斜杠和反斜杠则请移除本行代码
    $hash{$_}=1; # 标记该行已出现过
}
my @lines = keys(%hash); # 获得所有标记过的行
$" = "\n"; # 定义输出的分隔符是换行符
print("@lines"); # 输出


